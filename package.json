{
  "scripts": {
    "start": "npm run build; npm run server",
    "clean": "rm -rf ./bin",
    "build": "npm i; npm run clean; node_modules/.bin/tsc",
    "server": "killall -q webfsd; webfsd -F -l - -p 80 -r . -f index.html"
  },
  "devDependencies": {
    "typescript": "3.5.1"
  }
}
